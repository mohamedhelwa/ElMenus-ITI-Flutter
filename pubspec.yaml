name: ElMenus_ITI
description: El-Menus Flutter application Project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  carousel_slider: ^3.0.0
  smooth_star_rating: ^1.1.1
  flutter_spinkit: "^4.1.2"
  url_launcher: ^6.0.2
  flutter:
    sdk: flutter

  firebase_core: ^1.0.0
  cloud_firestore: ^1.0.0
  cupertino_icons: ^1.0.0
  firebase_auth: ^1.0.1
  webview_flutter: ^2.0.2
  fluttertoast: ^7.1.6


dev_dependencies:
  flutter_rating_bar: ^4.0.0
  flutter_svg: ^0.19.3
  sliding_up_panel: ^1.0.2
  provider: ^5.0.0

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/

  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
